{% extends 'default.html.twig' %}

{% block content %}
    <div class="aligncenter">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ error }}</h1>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-md-12">
                <a href="{{ base_url() }}" class="btn btn-primary btn-lg">
                    Go to home page
                </a>
            </div>
        </div>
    </div>
{% endblock %}
